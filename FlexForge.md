# 模块化数据管理系统（FlexForge）系统设计文档

**版本**：v1.0  
**状态**：概念设计稿（MVP 收敛见 `docs/`）  
**日期**：2026-08-19  

> 本文保留项目早期的完整设想。毕业设计实施时，以 `README.md` 和 `docs/` 下的可行性评审、需求规格、项目计划及架构文档为执行基线；超出 MVP 的内容仅作为未来扩展方向。

---

## 1. 项目概述

### 1.1 项目背景

随着生产企业数字化转型的深入，不同企业的业务流程和管理需求差异巨大，传统软件往往难以快速适配个性化需求，导致定制开发成本高、周期长、功能复用率低。同时，人工智能技术的快速发展为软件自动化开发提供了新的可能。

本项目旨在构建一个**高度可定制、插件化、模块化的数据管理系统**，通过元数据驱动、插件化业务模块和 AI 辅助生成能力，快速响应不同生产企业的定制化办公需求，降低研发成本，提高功能复用效率。

### 1.2 项目目标

- 建立一套**元数据驱动**的核心引擎，实现业务实体的动态定义与界面自动生成。
- 设计**插件化业务模块**机制，支持功能模块的独立开发、安装、卸载、启用与停用。
- 提供**可配置的自动化工作流**，支持业务动作的灵活编排。
- 实现 **Issue 需求管理**，形成从需求提出到功能上线的闭环。
- 引入 **AI Agent 与 Skill 机制**，辅助生成业务模块与插件，加速开发过程。
- 构建**插件市场**，支持插件的发布、下载与复用。

### 1.3 创新点

1. **AI 强提问需求澄清**：通过与 AI 的多轮对话，将模糊需求转化为结构化规格说明，减少沟通成本。
2. **Agent Issue 自动化开发流水线**：Issue 审核通过后自动触发 AI Agent 生成插件，实现从需求到可测试插件的自动化流转。
3. **插件化 + 元数据驱动**：业务功能以插件形式存在，同时底层数据模型与界面均通过元数据配置动态生成，实现高可复用性与灵活性。
4. **可复用 Skill 体系**：将通用能力封装为 Skill，接入 AI Agent，支持自然语言驱动的插件制作。

---

## 2. 需求分析

### 2.1 功能性需求

| 编号 | 功能模块 | 需求描述 |
|------|----------|----------|
| FR-01 | 登录系统 | 用户认证、JWT 令牌管理、单点登录支持 |
| FR-02 | 权限管理 | 基于 RBAC 的用户、角色、权限管理，细粒度接口与按钮权限控制 |
| FR-03 | 工作台 | 聚合待办事项、快捷入口、数据概览，支持个性化配置 |
| FR-04 | 业务模块 | 支持插件式业务模块的安装、卸载、启用/停用，菜单与路由自动注册 |
| FR-05 | ERP 能力 | 通过一组业务插件（如销售订单、库存管理）提供基础 ERP 功能，作为示例 |
| FR-06 | 自动化工作流 | 可配置触发器、条件、动作；支持数据变更触发、定时触发、按钮触发 |
| FR-07 | 需求管理（Issue） | 提交需求、评论讨论、状态流转、指派、标签管理 |
| FR-08 | 系统管理 | 用户管理、角色管理、菜单管理、系统日志 |
| FR-09 | 设置 | 系统参数、个性化偏好设置 |
| FR-10 | 插件市场 | 插件上传、下载、版本管理、详情展示、安装/卸载 |
| FR-11 | AI 辅助生成 | AI 对话式需求澄清、结构化需求生成、插件骨架自动生成 |

### 2.2 非功能性需求

- **可扩展性**：支持第三方插件开发与集成，插件间低耦合。
- **可维护性**：前后端分离，模块化组织，代码规范统一。
- **安全性**：身份认证、授权、数据隔离、操作审计。
- **易用性**：低代码配置界面，用户无需编程即可完成大部分定制。
- **性能**：核心接口响应时间 < 500ms，支持百级并发用户。
- **可部署性**：支持 Docker 容器化部署，数据库可选 MySQL/PostgreSQL。

---

## 3. 系统总体设计

### 3.1 系统架构

系统采用**前后端分离**的 B/S 架构，前端基于 Vue 3 + TypeScript，后端基于 Spring Boot 3 + MySQL/PostgreSQL。核心层包括元数据引擎、插件管理、工作流引擎、AI Agent 服务。
┌─────────────────────────────────────────────────────────┐
│ 前端应用（Vue 3） │
│ ├── 动态表单渲染器 ├── 动态列表渲染器 │
│ ├── 插件组件加载器 ├── 工作流设计器 │
│ └── 通用 UI 组件库（Element Plus / Ant Design Vue） │
└───────────────────────┬─────────────────────────────────┘
│ HTTP / WebSocket
┌───────────────────────▼─────────────────────────────────┐
│ 后端服务（Spring Boot） │
│ ├── 认证授权模块（Spring Security + JWT） │
│ ├── 元数据引擎（动态实体、动态 SQL、视图配置） │
│ ├── 插件管理模块（插件包解析、生命周期管理） │
│ ├── 工作流引擎（触发器、条件、动作执行） │
│ ├── Issue 管理模块 │
│ ├── 插件市场模块 │
│ └── AI Agent 服务（LLM API、Function Calling、Skill） │
└───────────────────────┬─────────────────────────────────┘
│
┌───────▼────────┐
│ 数据库 │
│ MySQL / PG │
└────────────────┘


### 3.2 技术选型

| 层次 | 技术 |
|------|------|
| 前端 | Vue 3、TypeScript、Vite、Pinia、Element Plus、动态表单渲染库 |
| 后端 | Spring Boot 3、Spring Security、MyBatis-Plus / JPA、JWT |
| 数据库 | MySQL 8.0 / PostgreSQL 14+ |
| 缓存 | Redis（可选，Token 缓存、热点数据） |
| AI 服务 | DeepSeek / OpenAI / Qwen API，Function Calling |
| 部署 | Docker、Nginx、Docker Compose |

---

## 4. 核心特性设计

### 4.1 元数据驱动引擎

**目标**：通过元数据配置动态生成业务实体的数据库表结构、后端 CRUD 接口和前端页面，实现零代码业务模块搭建。

**核心元数据表**：

- `meta_entity`：业务实体定义（表名、显示名称、描述）
- `meta_field`：字段定义（字段名、显示名、类型、长度、是否必填、默认值、校验规则）
- `meta_view`：视图配置（列表显示列、查询条件、表单布局）

**动态 SQL 生成**：根据元数据生成 `CREATE TABLE`、`SELECT`、`INSERT`、`UPDATE`、`DELETE` 语句，通过 MyBatis 动态执行。

**动态 GUI**：前端根据视图配置渲染表格列、查询表单、编辑表单，支持常见字段类型（文本、数字、日期、下拉、多选、附件等）。

### 4.2 插件化业务模块

**插件包结构**：
plugin-name/
├── plugin.json # 插件元信息（id、名称、版本、菜单、权限、依赖）
├── backend/ # 后端代码或配置
├── frontend/ # 前端组件或配置
├── migrations/ # 数据库初始化脚本
└── assets/ # 静态资源


**插件生命周期管理**：

- **安装**：解析 plugin.json，注册菜单、路由、权限，执行迁移脚本。
- **卸载**：清理菜单、路由、权限，删除或停用相关数据表。
- **启用/停用**：控制插件是否可用。

**前端加载**：通过动态 import 或 iframe 加载插件前端资源，实现组件隔离。

### 4.3 自定义 GUI

- 菜单自定义：用户可配置菜单项、排序、图标。
- 工作台自定义：拖拽配置快捷入口、数据卡片。
- 表单/列表自定义：基于元数据视图配置动态调整显示字段、布局和查询条件。

### 4.4 自动化工作流（Action）

**设计思路**：采用**轻量级动作链**模型，而非重型 BPMN 引擎，降低实现复杂度。

**组件**：

- **触发器**：数据新增、数据更新、定时任务、手动按钮触发。
- **条件**：字段值比较、逻辑与或非。
- **动作**：
  - 更新数据字段
  - 发送站内通知 / 邮件
  - 调用 HTTP 接口
  - 创建任务或 Issue
  - 触发其他工作流

**执行引擎**：监听数据库变更或定时扫描，匹配规则后执行动作链，记录执行日志。

### 4.5 Issue 需求管理系统

仿 GitHub Issue，支持：

- 标题、描述、标签、指派、里程碑
- 评论与讨论
- 状态流转：待处理 → 处理中 → 已完成 / 已关闭
- 与插件开发流程关联，自动化状态更新

### 4.6 插件市场

**功能**：

- 插件上传、下载
- 版本管理
- 详情展示
- 用户安装/卸载
- 管理员审核（可选）

**简化设计**：毕业设计阶段，插件市场可作为内部市场，允许用户上传和安装，不做复杂的沙箱与签名验证。

### 4.7 AI 辅助生成

**AI 对话式需求澄清**：AI 使用**强提问提示词**主动追问细节，将用户模糊需求转化为结构化 JSON 规格。

**Skill 体系**：

| Skill 名称 | 功能 |
|------------|------|
| `create_entity` | 创建业务实体 |
| `create_field` | 添加字段 |
| `create_view` | 生成视图配置 |
| `generate_plugin_manifest` | 生成插件描述文件 |
| `generate_crud_code` | 生成前后端 CRUD 代码骨架 |

**Agent 集成**：通过 Function Calling，Agent 根据需求规格调用相应 Skill，生成插件包，部署到测试环境。

---

## 5. Agent Issue 工作流设计

### 5.1 工作流概述

Agent Issue 是将 **AI 需求澄清、Issue 管理、Agent 自动化开发、人工审核测试** 结合的自动化需求处理流水线。

### 5.2 流程步骤

1. **需求澄清**  
   - 用户在系统内发起需求，进入 AI 对话界面。  
   - AI 通过强提问提示词追问字段类型、业务规则、权限、关联关系等。  
   - 生成结构化需求规格（JSON），供用户确认。

2. **自动创建 Issue**  
   - 用户确认后，系统自动创建 Issue，状态为 `待审核`。  
   - Issue 包含需求规格、对话摘要、预计插件信息。

3. **开发者审核**  
   - 开发者查看 Issue，可选择通过、修改或退回。  
   - 通过后状态变为 `已批准`，自动触发后续步骤。

4. **Agent 自动开发**  
   - 服务器 Agent 监听 `已批准` 状态，调用 AI 生成插件代码、前端组件、数据库脚本、plugin.json。  
   - 部署到测试环境，状态更新为 `待测试`。

5. **开发者测试**  
   - 开发者在测试环境测试插件，发现问题通过 Issue 评论反馈。  
   - 可手动修复或让 Agent 重新生成。  
   - 测试通过后状态变为 `测试通过`。

6. **合并发布**  
   - 管理员或系统自动将测试通过的插件合并到插件库。  
   - 插件正式上线，Issue 状态变为 `已完成`。

### 5.3 状态机
待审核 → 已批准 → 待测试 → 测试通过 → 已完成

↓ ↓ ↓
退回修改 开发失败 反馈修复


### 5.4 关键设计点

- **强提问提示词模板**：定义多轮对话的追问策略，确保需求完整性。
- **需求规格 Schema**：使用 JSON Schema 定义实体、字段、视图、权限等。
- **Agent 生成质量控制**：采用代码模板 + AI 生成，关键逻辑由模板保证。
- **环境隔离**：测试环境与生产环境分离。
- **权限控制**：不同角色（用户、开发者、管理员）拥有不同操作权限。

---

## 6. 数据库设计概要

### 6.1 核心表

| 表名 | 说明 |
|------|------|
| `sys_user` | 用户表 |
| `sys_role` | 角色表 |
| `sys_permission` | 权限表 |
| `sys_menu` | 菜单表 |
| `sys_user_role` | 用户角色关联 |
| `sys_role_permission` | 角色权限关联 |
| `meta_entity` | 业务实体定义 |
| `meta_field` | 业务字段定义 |
| `meta_view` | 视图配置 |
| `plugin_info` | 插件信息 |
| `plugin_version` | 插件版本 |
| `plugin_instance` | 已安装插件实例 |
| `workflow_definition` | 工作流定义 |
| `workflow_instance` | 工作流实例 |
| `workflow_action_log` | 动作执行日志 |
| `issue` | 需求/问题 |
| `issue_comment` | 评论 |
| `issue_label` | 标签 |
| `skill_definition` | Skill 定义 |
| `agent_task` | Agent 任务记录 |

### 6.2 关键字段示例

**meta_entity**

| 字段 | 类型 | 说明 |
|------|------|------|
| id | bigint | 主键 |
| name | varchar | 实体英文名（表名） |
| display_name | varchar | 显示名称 |
| description | varchar | 描述 |
| plugin_id | bigint | 所属插件 ID |

**meta_field**

| 字段 | 类型 | 说明 |
|------|------|------|
| id | bigint | 主键 |
| entity_id | bigint | 所属实体 ID |
| name | varchar | 字段名 |
| display_name | varchar | 显示名 |
| type | varchar | 字段类型（varchar/int/date/enum/file等） |
| required | boolean | 是否必填 |
| default_value | varchar | 默认值 |
| validation_rule | json | 校验规则 |

**issue**

| 字段 | 类型 | 说明 |
|------|------|------|
| id | bigint | 主键 |
| title | varchar | 标题 |
| description | text | 描述 |
| status | varchar | 状态（待审核/已批准/待测试/测试通过/已完成） |
| requirement_spec | json | 结构化需求规格 |
| assignee_id | bigint | 指派人 |
| plugin_id | bigint | 关联生成的插件 |

---

## 7. 系统模块划分

### 7.1 后端模块
flexforge-backend/
├── flexforge-common # 公共工具、统一返回、异常处理
├── flexforge-auth # 认证授权
├── flexforge-system # 用户、角色、菜单、日志
├── flexforge-meta # 元数据引擎
├── flexforge-plugin # 插件管理
├── flexforge-workflow # 工作流引擎
├── flexforge-issue # Issue 管理
├── flexforge-market # 插件市场
├── flexforge-ai # AI 服务、Skill、Agent
└── flexforge-web # 统一入口、Controller


### 7.2 前端模块
flexforge-frontend/
├── src/
│ ├── core/ # 核心框架（路由、状态、请求封装）
│ ├── components/ # 通用组件
│ ├── views/
│ │ ├── login/ # 登录
│ │ ├── dashboard/ # 工作台
│ │ ├── system/ # 系统管理
│ │ ├── meta/ # 元数据配置
│ │ ├── plugin/ # 插件管理
│ │ ├── workflow/ # 工作流设计
│ │ ├── issue/ # Issue 管理
│ │ └── market/ # 插件市场
│ └── dynamic/ # 动态渲染组件


---

## 8. 安全设计

- **认证**：JWT 无状态认证，密码 BCrypt 加密存储。
- **授权**：基于 RBAC，支持菜单级、接口级、按钮级权限控制。
- **数据隔离**：多租户场景下支持按企业 ID 数据隔离（可选）。
- **插件安全**：插件导入时校验 plugin.json 格式，限制可执行操作，仅允许受信任插件安装。
- **审计日志**：记录关键操作（登录、权限变更、插件安装、工作流触发）。
- **AI 输出安全**：AI 生成内容仅作为建议，必须经过人工确认和测试方可上线。

---

## 9. 部署方案

使用 Docker Compose 编排：

```yaml
services:
  frontend:
    image: nginx:alpine
    volumes:
      - ./frontend/dist:/usr/share/nginx/html
    ports:
      - "80:80"

  backend:
    image: openjdk:17
    volumes:
      - ./backend.jar:/app.jar
    command: java -jar /app.jar
    ports:
      - "8080:8080"
    environment:
      - DB_HOST=db
      - DB_PORT=5432

  db:
    image: postgres:14
    environment:
      POSTGRES_DB: flexforge
      POSTGRES_USER: flexforge
      POSTGRES_PASSWORD: secret
    volumes:
      - db_data:/var/lib/postgresql/data

volumes:
  db_data:
```

###  10. 风险分析与应对
| 风险 | 影响 | 应对措施 |
|------|----------|----------|
|元数据引擎性能问题 | 动态 SQL 可能较慢 | 增加缓存，预编译 SQL，限制单表字段数量 |
|AI 生成代码质量不稳定 | 插件功能异常 | 采用代码模板 + AI 混合生成，强制人工测试 |
|插件安全漏洞 | 系统被攻击 | 仅允许受信任插件，插件权限最小化，审计日志 |
|工作流设计复杂 | 开发周期延长 | 采用轻量级动作链，不做图形化 BPMN |
|需求范围过大 | 无法按期完成 | 严格按 MVP 裁剪，ERP 仅做示例模块 |
|Agent 输出不可控 | 生成结果不符合预期 | 多轮确认，结构化输出，人工审核兜底 |

###  11. 未来扩展方向
- 多租户支持，面向 SaaS 模式。

- 可视化拖拽式 GUI 设计器。

- 更强大的工作流引擎，支持分支、会签、并行。

- 插件市场开放生态，支持开发者远程发布。

- 基于大模型的智能数据分析与报表生成。

- 移动端适配与小程序支持。

###  12. 结语
本系统以模块化和插件化为核心，结合元数据驱动、自动化工作流、Issue 需求管理和 AI 辅助生成，旨在打造一个高度可定制的企业级数据管理平台。通过 Agent Issue 工作流，实现从需求到可测试插件的自动化流水线，显著提升研发效率与功能复用率，具有较高的工程价值和创新意义。

文档编制：设计阶段文档，后续可根据开发进展持续更新。
