/**
 * 动态数据访问模块（service.data-access 责任模块，docs/08 §2）：单 JSONB 记录表存储、
 * 实体级记录校验编排、白名单 SQL 构造与动态 CRUD API。
 * 分层 api → application → domain → infrastructure（docs/10 R1）。
 */
package com.flexforge.data;
